# Dependabot tracks this image tag and opens PRs when new versions are
# available.  CI workflows extract the static busybox binary at build
# time using this Dockerfile — no committed binary is needed.
#
# Keep this version aligned with the FROM busybox line in initrd.Dockerfile.
FROM busybox:1.37.0-musl
