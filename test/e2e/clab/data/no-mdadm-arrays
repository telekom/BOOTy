#!/bin/sh
if [ "$1" = "--stop" ] && [ "$2" = "--scan" ]; then
    echo "mdadm: No arrays found in config file or automatically" >&2
    exit 1
fi

echo "test mdadm stub only supports --stop --scan" >&2
exit 1
