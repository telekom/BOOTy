! leaf02 — pure L3 router for VM1 in Type-5 e2e topology.
! Forwards EVPN + IPv4 unicast between VM1 and spine01.
! No VXLAN, no bridge — just routing.
!
frr version 10.3.2
frr defaults datacenter
hostname leaf02
log syslog informational
service integrated-vtysh-config
!
interface lo
 ip address 10.0.0.3/32
exit
!
interface eth1
 ipv6 nd ra-interval 10
 no ipv6 nd suppress-ra
exit
!
interface eth2
 ipv6 nd ra-interval 10
 no ipv6 nd suppress-ra
exit
!
router bgp 65002
 bgp router-id 10.0.0.3
 no bgp ebgp-requires-policy
 bgp bestpath as-path multipath-relax
 !
 neighbor fabric peer-group
 neighbor fabric remote-as external
 neighbor eth1 interface peer-group fabric
 neighbor eth2 interface peer-group fabric
 !
 address-family ipv4 unicast
  neighbor fabric activate
  neighbor eth2 prefix-list vm1-vtep in
  redistribute connected
 exit-address-family
 !
 address-family l2vpn evpn
  neighbor fabric activate
 exit-address-family
exit
!
ip prefix-list vm1-vtep seq 5 permit 192.168.4.11/32
!
end
